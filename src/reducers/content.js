const contentReducer = (state = {},action)=>{
    switch(action.type)
    {
        case 'DETAIL':
            return state;
        default:
            return state;
    }
}

export default contentReducer;